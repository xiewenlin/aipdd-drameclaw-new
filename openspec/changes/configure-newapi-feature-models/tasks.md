## 1. 配置模型与持久化

- [x] 1.1 为 `settings.db` 增加版本化的自定义 NewAPI 连接、同步状态、模型目录和功能绑定结构
- [x] 1.2 实现 NewAPI Base URL 规范化和端点拼接测试，覆盖有无 `/v1` 与尾斜杠的输入
- [x] 1.3 实现 API Key 脱敏读取、留空不替换和 Authorization 日志过滤
- [x] 1.4 定义模型能力枚举、模型目录记录及增量同步合并规则

## 2. 功能目录与后端 API

- [x] 2.1 建立后端唯一功能目录，登记文本、视觉文本、图片、视频、音频和 Embedding 功能
- [x] 2.2 实现 NewAPI 配置读取、保存和连接测试 API
- [x] 2.3 实现 `/models` 同步、目录查询、不可用标记和同步错误保留 API
- [x] 2.4 实现模型多能力手动标注 API，并加入名称/元数据启发式推断
- [x] 2.5 实现功能目录、功能绑定读取保存和配置完整性校验 API
- [x] 2.6 实现按功能执行最小协议请求的测试 API
- [x] 2.7 从服务端配置返回可选 `official_service_url`，且不接入任何运行时分支

## 3. 配置页改造

- [x] 3.1 将 official/custom 和 Provider 渠道界面替换为单一 NewAPI 地址、Key、测试与同步区域
- [x] 3.2 展示连接状态、最近测试时间、最近同步时间和可操作错误信息
- [x] 3.3 根据后端功能目录动态渲染功能模型配置，不再维护前端重复功能模型常量
- [x] 3.4 为每个功能实现推荐模型和全部模型两级下拉，允许选择 `unknown` 模型
- [x] 3.5 展示模型不可用、能力不匹配和必需功能未配置警告，但不阻止手动跨类型保存
- [x] 3.6 增加模型能力手动编辑、功能测试入口和可选官方服务外链

## 4. 统一运行时解析

- [x] 4.1 实现只读取全局功能绑定的 `ModelRuntimeResolver`，拒绝或忽略请求和项目级模型覆盖
- [x] 4.2 定义缺少连接、缺少绑定和模型不可用的结构化运行错误
- [x] 4.3 将 PydanticAI 文本模型构建改为按功能 ID 获取 NewAPI 连接与模型
- [x] 4.4 为旧文本 Agent 分配明确功能 ID，移除通用 `MODEL_NAME` 的隐藏选择
- [x] 4.5 在配置更新后使 Resolver 和 Agent 缓存安全失效，并记录需要重启的组件

## 5. 媒体与 Embedding 路由

- [x] 5.1 将草图、渲染图、场景图、360 图和道具图调用迁移到图片功能绑定
- [x] 5.2 将文生视频、图生视频及任务轮询迁移到视频功能绑定
- [x] 5.3 将 IndexTTS2 云端调用迁移到音频功能绑定和 NewAPI 凭证
- [x] 5.4 将 Cognee Embedding 迁移到 Embedding 功能绑定并处理进程级重载提示
- [x] 5.5 保留 ComfyUI 等明确本地 Transport，并阻止其重新启用云 Provider 直连
- [x] 5.6 为各协议适配器增加模型 ID、端点和错误响应的合同测试

## 6. 历史配置迁移

- [x] 6.1 实现旧逻辑模型、图片 selection、视频 backend、音频和 Embedding 配置扫描器
- [x] 6.2 实现 dry-run 报告，区分可映射、不可映射、已失效和需人工确认的值
- [x] 6.3 实现全局功能绑定迁移，备份后移除或停止读取历史项目模型选择字段
- [x] 6.4 为配置版本升级、重复执行和迁移回滚增加测试

## 7. 旧渠道退役

- [ ] 7.1 增加分能力切换开关，依次启用文本、图片、视频、音频和 Embedding 新路由
- [x] 7.2 移除官方渠道的鉴权、默认映射和运行时回退，仅保留普通外链
- [x] 7.3 移除前端 Provider 渠道管理以及云 Provider Key/Base URL 配置入口
- [ ] 7.4 在兼容期结束后删除 OpenAI、OpenRouter、Google、HuiMeng、Ark、DashScope、Grok 和 FAL 云端直连读取路径
- [x] 7.5 更新 `.env.example`、部署文档和用户迁移说明

## 8. 验证与发布

- [x] 8.1 增加连接、同步、分类、绑定和 Resolver 的后端单元与 API 合同测试
- [ ] 8.2 增加配置页交互测试，覆盖未知模型、全部模型、失效模型和 Key 保留行为
- [ ] 8.3 使用测试 NewAPI 完成文本、图片、视频、音频和 Embedding 最小冒烟测试
- [ ] 8.4 验证历史项目 dry-run、迁移、任务执行和备份回滚流程
- [ ] 8.5 运行默认测试、前端检查和 gitleaks，并记录仍需人工确认的 NewAPI 协议差异
