# configure-newapi-feature-models

Replace official runtime channels with a user-configured NewAPI endpoint, synchronize its models, and bind models to text, image, video, audio, and embedding features.
